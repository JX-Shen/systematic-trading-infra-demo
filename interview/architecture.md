# Public Architecture Note

This path is kept for compatibility with validation commands that scan older
documentation locations.

The maintained public architecture document is:

```text
docs/architecture.md
```

That document describes the local fixture replay, strategy intent boundary,
portfolio target state, `RiskGate`, simulated provider adapter,
provider-confirmed callbacks, reconciliation reports, and JSONL trace review in
public-safe language.
